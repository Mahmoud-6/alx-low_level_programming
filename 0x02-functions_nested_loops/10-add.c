#include <stdio.h>
#include "main.h"
/**
* add - function that prints result of adding two numbers
* @a: parameter that contains a value
* @b: parameter that contains a value
* Return: result
*/
int add(int a, int b)
{
int result = a + b;
return (result);
}
