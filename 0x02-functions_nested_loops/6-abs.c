#include <stdio.h>
#include "main.h"
/**
* _abs - function that returns absolute
* @n: parameters that contains the value
* Return: 0
*/
int _abs(int n)
{
if (n < 0)
{
n = n * (-1);
return (n);
}
else
{
return (n);
}
}
